meta {
  name: 00 - Health check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

tests {
  test("database is healthy", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property("db", "ok");
  });
}
